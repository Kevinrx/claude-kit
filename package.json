{
  "name": "claude-kit",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "validate": "claude plugin validate plugins/kit && claude plugin validate .",
    "setup": "node setup/setup.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
